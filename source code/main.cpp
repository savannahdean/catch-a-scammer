//this is our main file

/*

this will only be used for creating the raylib window i believe. Then we start handling everything in the core file.

*/

int main() {

    //set up window & fps

    //while loop for if the window should close (the main program loop. the code runs here).
        //draw title screen.
            //the following will be handled in a seperate file
            //if start is pressed, title screen = false and gameloop = true.
                //game.gameloopSTART();
                    //this should be handled in a completely seperate file.
                    //we should never come back here i believe unless we close the window. 
                    //then it comes back for some cleanup and will return with 0.
        //for the exit button, i believe we can just do exit(0) and the program will close all by itself.



    return 0;
}